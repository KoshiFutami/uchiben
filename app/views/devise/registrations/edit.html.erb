
  <div class="row">
    <div class="col-6">
      <h2>プロフィールを編集</h2>
      
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>
      
        <div class="form-group">
          <%= f.label :username, value: 'ユーザー名' %>
          <%= f.text_field :username, autofocus: true, class: 'form-control' %>
        </div>
      
        <div class="form-group">
          <%= f.label :name, value: '名前' %>
          <%= f.text_field :name, autofocus: true, class: 'form-control' %>
        </div>
      
        <div class="form-group">
          <%= f.label :email, value: 'メールアドレス' %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class: 'form-control' %>
        </div>
      
        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>
      
        <div class="form-group">
          <%= f.label :profile, value: '自己紹介' %>
          <%= f.text_area :profile, autofocus: true, rows: '5', class: 'form-control' %>
        </div>
      
        <div class="form-group">
          <%= f.label :avatar, value: 'プロフィール写真' %>
          <%= f.file_field :avatar, autofocus: true %>
          <%# if @user.avatar %>
            <%#= image_tag @user.avatar, alt: @user.name %>
          <%# end %>
        </div>
      
        <div class="form-group">
          <%= f.label :password, value: '新しいパスワード' %> <small>※変更しない場合は空欄のままでOK</small>
          <%= f.password_field :password, autocomplete: "new-password", class: 'form-control' %>
          <% if @minimum_password_length %>
            <br />
            <small>※<%= @minimum_password_length %>文字以上</small>
          <% end %>
        </div>
      
        <div class="form-group">
          <%= f.label :password_confirmation, value: '新しいパスワード（確認用）' %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password", class: 'form-control' %>
        </div>
      
        <div class="form-group">
          <%= f.label :current_password, value: '現在のパスワード' %> <small>※登録内容の変更のために入力必須</small>
          <%= f.password_field :current_password, autocomplete: "current-password", class: 'form-control' %>
        </div>
      
        <div class="actions">
          <%= f.submit "保存", class: 'btn btn-primary' %>
        </div>
      <% end %>
    </div>
  </div>
  
  <div class="row">
    <div class="col-6">
      <div class="border p-4 mt-5">
        <h3 class="h4">アカウントを削除</h3>
        
        <p>うちなー塾アカウントの削除プロセスを開始します。 <%= button_to "アカウント削除", registration_path(resource_name), data: { confirm: "本当に削除してもよろしいですか？" }, method: :delete, class: 'btn btn-outline-danger' %></p>
      </div>
    </div ckas>
  </div>
  
  <div class="mt-5 text-right"><%= link_to "戻る", :back %></div>

